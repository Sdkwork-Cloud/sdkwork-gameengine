Locale seed directories:

- `zh-CN/` - default active locale.
- `en-US/`, `ja-JP/`, `de-DE/`, `fr-FR/`, `ru-RU/`, `ko-KR/` - inactive locale directories kept for seed manifest parity.

Only active locale directories contain ordered SQL seed files referenced by `seeds/seed.manifest.json`.
