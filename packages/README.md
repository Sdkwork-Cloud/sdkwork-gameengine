# Packages

Shared TypeScript contracts consumed across the games workspace.

| Package | Purpose |
| --- | --- |
| `packages/common/games/sdkwork-games-contracts` | Cross-surface DTOs and helpers using `@sdkwork/utils` |

Architecture-local packages for PC UI live under `apps/sdkwork-gameengine-pc/packages/`.
