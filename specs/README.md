# Component Specs

Local component contracts for `sdkwork-games`. Root standards remain authoritative in `../sdkwork-specs/`.

- `component.spec.json`: workspace component identity, canonical spec links, and verification hooks.
