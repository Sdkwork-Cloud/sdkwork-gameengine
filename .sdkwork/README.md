# SDKWork Workspace

Source-controlled workspace metadata for `sdkwork-games`, governed by `../sdkwork-specs/SDKWORK_WORKSPACE_SPEC.md`.

- `skills/`: repository-local agent skills
- `plugins/`: repository-local plugins

This directory is not runtime state.
