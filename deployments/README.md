# Deployments

Deployment descriptors and packaging handoff for `sdkwork-games`.

Supported profiles: `standalone`, `cloud` (see `sdkwork.app.config.json`).

No RPC split-service deployment yet; `sdkwork-discovery` is deferred until gRPC services are introduced.
